name: Testing

on:
  push:
    branches-ignore:
      - master

jobs:
  unittests:
    runs-on: ubuntu-latest
    steps:
      - name: error
        run: echo "::error file=/app/script.js,line=10,col=15::Something went wrong"
